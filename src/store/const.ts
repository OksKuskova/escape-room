export enum SliceName {
  Quests = 'quests',
  FullQuest = 'full-quest',
  User = 'user',
}
export enum ApiRoute {
  Quest = 'quest',
  Login = 'login',
  Logout = 'logout',
}

export enum RequestStatus {
  Idle = 'idle',
  Loading = 'loading',
  Success = 'success',
  Failed = 'failed',
}
